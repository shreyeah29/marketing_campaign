# A package must declare every dependency it imports — this keeps modules
# independently extractable and prevents accidental coupling through hoisting.
hoist=false
strict-peer-dependencies=false
auto-install-peers=true
enable-pre-post-scripts=true
prefer-workspace-packages=true
link-workspace-packages=deep
